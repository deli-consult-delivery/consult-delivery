---
context: default
status: in_progress
last_updated: 2026-05-05
---

## Sessão — Bugs Chat ao Vivo (continuação)

<current_state>
Bug fixing avulso no ChatScreen e evolution-webhook. Sem fase GSD ativa.
Todos os commits foram direto no main.
</current_state>

<completed_work>

- verify_jwt=false para evolution-webhook, persist-profile-pic, analista-callback (config.toml)
- Evolution API: migrado evo-go → evo1 (DB + secrets Supabase + .env.local + GitHub secrets)
- Sidebar scroll: overflow:hidden + minHeight:0 na Col1
- Duplicatas sidebar: limit(1) + dedup por whatsapp_chat_id + Realtime handler
- F5 persiste rota: localStorage cd-route
- Status conversa finalizada reabre ao receber mensagem (status_v2 reset)
- Foto de perfil: secrets EVOLUTION_URL/KEY atualizados, 400→sem foto, race condition photoCacheRef
- height:100% no ChatScreen root (input visível)
- Grupos aparecendo como PV: pushName não mais usado como group_name; upsertGroup sincroniza group_name real; is_group corrigido
- Foto de perfil de grupos: ChatScreen inclui type==='group' no loop auto-persist; persist-profile-pic preserva dash no JID de grupo
- Mensagens duplicadas ao enviar pela plataforma: handleSendMessage faz dedup por conteúdo recente (strip prefixo *Nome:*\n) antes de inserir
- Mensagens do celular físico não sincronizavam: handleMessagesUpsert agora processa fromMe=true com duplo dedup em vez de return imediato
- Assinatura "Equipe" removida de mensagens outbound do webhook (sender_name: null)
</completed_work>

<remaining_work>

- Marca d'água nas fotos de perfil: WATERMARK_ON_MESSAGES=false no EasyPanel não funcionou — precisa investigar (talvez restart do container ou outro param)
- Bug input sumindo a 100% zoom: fix deployado (height:'100%') mas não confirmado pelo usuário
- Bug status conversa finalizada: fix deployado mas não confirmado pelo usuário
- Foto sem marca d'água: push_photo_url foi limpo do banco; fotos antigas no Storage ainda têm watermark — re-fetch ocorre automaticamente no próximo carregamento
</remaining_work>

<decisions_made>

- evo-go removido completamente; apenas evo1-evolution-api.bawafu.easypanel.host usado
- Instância "teste" deletada do banco; 3 conversas migradas para consult-delivery
- Groups usam overwriteName=false para não sobrescrever nome real com JID do remetente
- scrollbar-gutter:stable revertido (comportamento de scroll era desejado pelo usuário)
- Mensagens do celular físico chegam via MESSAGES_UPSERT fromMe=true (não SEND_MESSAGE)
- sender_name=null para mensagens outbound do webhook (sem assinatura automática)
</decisions_made>

<blockers>

- Marca d'água: causa ainda desconhecida; WATERMARK_ON_MESSAGES=false foi configurado mas sem efeito — próxima sessão deve investigar logs do container ou tentar restart
</blockers>

## Infrastructure State

- Branch: main (todos commits direto no main — sem PR nesta sessão)
- Edge Functions deployadas (versões atuais): evolution-webhook v21, persist-profile-pic v11
- Supabase secrets: EVOLUTION_URL e EVOLUTION_KEY corretos (evo1)
- GitHub secrets: VITE_EVOLUTION_URL e VITE_EVOLUTION_KEY atualizados para evo1

## Commits desta sessão

- 353f8d1 feat(profile-pic): buscar foto de perfil também para grupos
- a6dd104 fix(chat): dedup mensagens duplicadas ao enviar pela plataforma
- f38a6df fix(webhook): sincronizar mensagens enviadas pelo celular físico
- 8db43ed fix(webhook): remover assinatura 'Equipe' de mensagens outbound do celular

<next_action>
Confirmar com usuário: (1) input visível a 100% zoom? (2) status conversa finalizada reabre ao receber msg? (3) marca d'água — investigar restart do container EasyPanel ou variável alternativa
</next_action>

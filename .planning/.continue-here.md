---
context: default
status: paused
last_updated: 2026-05-07T03:30:00Z
---

<current_state>
Sessão ad-hoc de integração LARA ↔ EvoNexus. Sem phase GSD ativa.
Bridge Server deployado na VPS com tickets API do EvoNexus funcionando.
Falta: atribuir agente ao Trigger 3 (pesquisa) no painel EvoNexus, depois testar polling completo.
</current_state>

<completed_work>

- LARA diagnosticada: agente ativo no OpenClaw, todos os blockers identificados
- NEXUS_API_KEY + NEXUS_BASE_URL + NEXUS_TICKET_TOKEN + NEXUS_TICKET_BASE configurados no .env da VPS
- Loja "Varanda's Restaurante & Pizzaria" criada na tabela `lojas` (id: 6a8c6978-8575-45a2-b971-00bd9a81c754)
- Migration Supabase: trigger auto-cria `loja` ao inserir `customer` no CRM
- Descobertos endpoints reais do EvoNexus: tickets API em `/api/tickets`
- bridge-server/index.js atualizado: `POST /api/nexus-dispatch/:agent` agora cria ticket no EvoNexus com `assignee_agent`
- Mapeamento: pesquisa→pixel-social-media, regua→clawdia-assistant, midia→pixel-social-media
- TOOLS.md da LARA atualizado com URLs HTTPS, token, templates curl de disparo e polling
- system_prompt.md Etapas 2/3/4 atualizadas: curl concreto em vez de "via tool"
- Migração para HTTPS: NEXUS_TICKET_BASE=https://ia.consultdelivery.com.br (Let's Encrypt)
- Teste E2E passou: dispatch pesquisa → ticket_id retornado → EvoNexus cria ticket
</completed_work>

<remaining_work>

**AÇÃO HUMANA NECESSÁRIA (blocker):**
- Acessar https://ia.consultdelivery.com.br → Triggers → Trigger 3 (pesquisa) → atribuir agente
  (hoje `agent: null` — sem isso o Nexus-Pesquisa não processa nada)

**Técnico (próxima sessão):**
- Confirmar agentes corretos para cada tipo (pesquisa/régua/mídia) com Wandson
- Testar polling completo: criar ticket real → aguardar `status=resolved` → ler `comments`
- Ajustar mapeamento no bridge se agentes estiverem errados
- Teste E2E completo pela tela da LARA (frontend → Bridge → OpenClaw → EvoNexus)
- Commitar bridge-server/index.js (modificado, não commitado)
</remaining_work>

<decisions_made>

- Usar tickets API do EvoNexus (não triggers webhook): tickets permitem polling de status + leitura do resultado via GET /api/tickets/{id} e campo `comments`
- Bridge cria ticket com `assignee_agent` mapeado por tipo: pesquisa→pixel-social-media, regua→clawdia-assistant, midia→pixel-social-media
- LARA usa bash/curl para disparar (não chama outro agente OpenClaw)
- Trigger banco Supabase sincroniza customers→lojas automaticamente
</decisions_made>

## Infrastructure State

- **bridge-server:** active VPS 45.39.210.183:3001 (systemd), REINICIADO com novos env vars
- **openclaw:** active VPS porta 18789 (systemd)
- **LARA:** ativa no OpenClaw, workspace/system_prompt.md + workspace/TOOLS.md atualizados
- **EvoNexus:** https://ia.consultdelivery.com.br — HTTPS Let's Encrypt, token `nC5Re2whwT3KgMYkcJVJ1oWMRTz2nsqb2W_pX4dmr6c`
- **Supabase:** migrations lara_regua + nexus_requests aplicadas, trigger auto-loja ativo
- **Arquivo não commitado:** `bridge-server/index.js`

## Required Reading (ao retomar)

1. `.planning/HANDOFF.json` — estado completo estruturado
2. `bridge-server/index.js` linhas 148-195 — lógica do nexus-dispatch com tickets API
3. `/root/.openclaw/agents/lara/workspace/TOOLS.md` na VPS — endpoints e scripts de polling

<next_action>
1. Wandson confirma que atribuiu agente ao Trigger 3 no EvoNexus
2. Disparar pesquisa real pela tela da LARA com Varanda's
3. Fazer polling manual: `curl -H "Authorization: Bearer nC5Re..." https://ia.consultdelivery.com.br/api/tickets/{ticket_id}` até status=resolved
4. Ler campo `comments` do ticket para confirmar resultado chega
5. Commitar bridge-server/index.js
</next_action>

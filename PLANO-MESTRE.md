# 🗺️ PLANO-MESTRE — Consult Delivery (Mapa Vivo)

> **Versão 2** (2026-06-06) — fusão do `PLANO-MESTRE.md` (EvoNexus-replica, 2026-06-02) com o `PLANO-MESTRE-mapa-vivo.md` (versão-semente, 2026-06-06). Doc autoritativo do projeto: `RESTRUCTURE.md` (raiz) — em divergência, RESTRUCTURE.md vence.

---

## 0. Como usar este documento

- **Fonte única da verdade.** Toda sessão (chat ou Claude Code) **lê isto PRIMEIRO** e **atualiza por ÚLTIMO**.
- Nunca apagar item concluído — marca `[x]` e mantém o histórico.
- Status só vira ✅ **com evidência** (commit hash, output bruto, teste). Sem evidência = `(memória·confirmar)`.
- Se uma sessão de IA propuser algo, ela diz **em qual track/item** entra — pra você nunca perder o fio.

**Legenda:** ✅ feito · 🔄 em andamento · ⏳ pendente · 🔒 bloqueado · 👉 próximo · ⚠️ risco/atenção

---

## 👉 PRÓXIMA AÇÃO (retomar aqui)

0-CONTINUIDADE. **✅ LEVA 3 (05/07 tarde) — gates do Wandson executados, todos com aprovação explícita dele hoje.** #745 listagens de lojas filtram `is_contato=false` (deploy confirmado) · higienização de lojas/contatos APLICADA em prod via MCP (1.028 contatos criados, 1.028 lojas flageadas, 150 lojas reais, 1.178 total intacto, zero DELETE) · Bridge da VPS atualizado e reiniciado (main `f413e68`, rotas `/api/ifood-api/*` vivas) · smokes visuais CRM+Lojas aprovados · **#746 SQUASH BASELINE B1 100% CONCLUÍDO** (dump read-only do prod validado em banco zerado, 238 migrations arquivadas em `supabase/migrations_archive/`, `schema_migrations`=1 linha, schema/dados intactos; regra pós-squash: migrations novas seguem versionadas normal). Isso fecha as pendências da consolidação da madrugada 04→05/07 — 14 PRs (Semana 1 do `docs/estrategia/PLANO-CONTINUIDADE-PLATAFORMA-2026-07.md`) + LEVA 2 (5 PRs #740-#743 + migration), GAPs 1-8 do T3 100% fechados (auditoria #726). Detalhe completo: `WikiBrain/wiki/PLANO-MESTRE — Tracker.md` (Onde parou + Log de sessões, 2026-07-05 tarde/dia/madrugada). **🛑 Única pendência restante do plano:** credenciais `IFOOD_CLIENT_ID`/`IFOOD_CLIENT_SECRET` (o Wandson vai passar) → teste live sandbox. **Semana 4+:** convergência F3 da coleta (aguarda validação do dicionário no Cowork). Frente A (homologação iFood API oficial) segue conforme a sequência travada do plano.

0-AI-FIRST. **🛑 AI-First — Blueprint escrito, aguardando aprovação do Wandson p/ FASE 1 (sessão 91, 2026-06-23).** O plano-mestre AI-First está em **`docs/ai-first/BLUEPRINT-AI-FIRST.md`** (PT-BR, v1, status PROPOSTA). Consolida a visão (cliente→especialista→**tarefa em pipeline com visão tempo-real**→resolução em sistema externo→resposta ao cliente; **DELI = o Oráculo**, começando no **Telegram**) e as 4 decisões travadas (AF-1 blueprint-primeiro · AF-2 Ernesto=DELI · AF-3 Telegram primeiro · AF-4 o Loop é a 1ª fase). Inventário confirma que a plataforma já tem a maioria dos blocos (memória `client_facts`/`client_timeline`, especialistas em `trigger/`, DELI+semáforo, Oracle agent-builder, Hermes/Telegram+admin-mcp, drafts+aprovação, `client_tasks`+Realtime parcial, Console v2); gaps = loop não cabeado · tela Pipeline · Realtime incompleto · `/agents/deli/notify` · `notifyBridge` comentado. **Próximo passo:** Wandson lê o blueprint e aprova a FASE 1 (o Loop). Ao aprovar, 1ª fatia = `supabase/migrations/20260623_001_loop_core.sql` (aditiva/reversível, **autônoma**) + helper `createLoopTask()` + estender `trigger/breno/responder.ts` (saída discriminada resolver|criar_tarefa). **Constraint:** motor EvoNexus PROIBIDO em prod (só o paradigma); nenhuma mensagem a cliente sem aprovação. Nada de produção/cliente tocado nesta sessão.

0a. **VendaERP — FASE 2 (escrita propor→confirmar): GATE 0 do Wandson** (T4/T6, sessão 54). Código IMPLEMENTADO E COMMITADO na branch `wandson/vendaerp-fase2-escrita` (5 tools `erp_propor_*` + `erp_confirmar`, 5 rotas POST no Bridge sem retry, migration `20260614_003_vendaerp_proposals.sql`, smokes offline 6/6 + Bridge 14/14). **Reservado ao Wandson (secrets/VPS):** (a) `systemctl restart hermes-gateway` (carrega as tools novas só em start limpo) + sessão NOVA do @DeliConsultBot; (b) `cd vendaerp-mcp && npm run write-live-smoke` → output bruto (proposta `executed` + `audit_log mcp:erp_confirmar ok=true`); (c) E2E Telegram (propõe→pergunta "Confirma? sim/não"→"sim"→grava); (d) **rotação do `VENDAERP_TOKEN`** (pendente desde a sessão 52). Depois: Fase 3 (multi-tenant). Dívidas técnicas no Tracker §15b.
0. **🏁 ÉPICO DASHBOARD iFOOD — COMPLETO (Fases 0–6 entregues).** Próxima ação deste épico = **nenhuma — QA empírico E2E ENCERRADO (sessão 58, ver abaixo); 🏁 épico 100% entregue, em prod e VERIFICADO por output bruto.** Resta só aguardar o Wandson subir uma planilha `.xlsx` real para destravar o **Balde 3** (quebra fina por colunas — gated por P1, não chutar layout), ou novo pedido. · **Polimento pós-épico (sessão 56, #361 `9559cfa`):** endurecimento defense-in-depth de `aprovarTarefa`/`rejeitarTarefa` (`src/lib/api.js` + 2 call-sites no `RadarReal.jsx`) — a transição passa a ser escopada por `loja_id` (`.eq('loja_id', lojaId)`) além de `id`+`status='rascunho'`+`criado_por_ia`, fechando a brecha teórica de aprovar/rejeitar rascunho de outra loja; deploy frontend (bundle `index-CID8E-Vg.js`), QA por string OK, sem regressão. · **✅ QA empírico de fim a fim da Fase 6 — ENCERRADO (sessão 58):** Wandson disparou o cron `radar-diagnostico-semanal` no painel Trigger.dev (run `run_cmqemdi4g005b0nn2vx8agals`, COMPLETED/isSuccess, API v3) → **METADE 1:** 6 rascunhos para a loja `8434cea4-b9c8-41ea-b366-57e8398aad0b` (`criado_por_ia=true`/`status='rascunho'`/`metadata.fonte='radar-diagnostico-semanal'`/`aprovada_em=null`; sinais carga_ifood·conversao_baixa·subsidios·cancelamentos·op_atrasos·op_chamados — op_online & op_canc_super não dispararam, correto). **METADE 2:** clicou Aprovar em "Tratar 7 cancelamento(s)" → `status='aprovada'`+`aprovada_em='2026-06-15 03:14:04+00'`, os outros 5 intactos em `rascunho`. **3/3 critérios de aceite provados por output bruto** (rascunho na loja certa · aprovar move status · nada vira tarefa valendo sem o clique do Wandson). **🏁 SEM pendência — épico 100% verificado.** · **FASE 6 ✅ ENTREGUE (sessão 55):** demandas como **rascunho automático + aprovação** em `tarefas_loja` (sem migration de schema — `status DEFAULT 'rascunho'` já existia). `diagnostico-semanal.ts` deriva até **8 sinais** das métricas EAV (`carga_ifood`, `conversao_baixa`, `subsidios`, `cancelamentos`, `op_atrasos`, `op_online`, `op_canc_super`, `op_chamados`) e cria linhas com `status='rascunho'`, `criado_por_ia=true`, `bloco`/`prioridade` mapeados, NOT NULL preenchidos, `metadata.origem` p/ dedup; dedup SELECT-then-filter por origem não-terminal + **backstop de idempotência** (índice único parcial `uq_tarefa_ia_origem_ativa`, migration `20260620_002` APLICADA) tratando `23505` como no-op; painel **"Ações recomendadas"** no `RadarReal.jsx` (rascunhos com Aprovar/Rejeitar + acompanhamento por status) sobre `listTarefasIA`/`aprovarTarefa`/`rejeitarTarefa` em `api.js` (update guardado por `.eq('status','rascunho')` — RLS). Review LOCAL cd-lens xhigh 4/5 tratados (dívida aceita do `cargaPct`); tsc/vite EXIT 0. Aceite cumprido: rodar o diagnóstico → rascunhos na loja certa; aprovar move o status; nada vira tarefa "valendo" sem o clique do Wandson. Plano: `/root/.claude/plans/dynamic-swimming-zebra.md`. · **FASE 5 ✅ ENTREGUE (sessão 53, #356): série diária de verdade em `radar_series` onde há grão fino** — Migration B aplicada (`{"success":true}`) + RLS de isolamento provado; parser grava 6 métricas de Operação por dia (`diasDaSerie`, DELETE-por-`fonte_id` idempotente); helper `src/lib/radar-series.js` (`lerSeries`+`agregar` dia/semana/mês client-side) + componente `SerieDiaria` (barras CSS, sem dependência nova); **Vendas/Cardápio/Conciliação continuam sem série** (iFood entrega agregado — P1); review LOCAL cd-lens 3 bugs corrigidos; tsc/vite EXIT 0 + 6/6 testes. · **FASE 1 ✅** (helper único de dedup) · **FASE 2 ✅ EM PROD E VALIDADA** (#349, deploy `20260614.33`) · **FASE 3 ✅** (#351, grid "Saúde financeira & cruzamentos", 7 KPIs) · **FASE 4 ✅ ENTREGUE (sessão 51, só frontend — sem migration/sem deploy):** filtro temporal universal no `RadarReal.jsx` — barra de período (Tudo·Dia·Semana·Mês·Personalizado) com botões-janela `.cv2-btn`/`.cv2-btn.sec` + date-range custom; passa `periodo` ao helper `lerMetricas` (ramo `data_ref`, dormente desde a Fase 1); aviso honesto inline ("o iFood entrega agregado por período — o filtro recorta pela data de referência") + banner honesto quando a janela não tem relatório (nunca tela em branco, nunca gráfico fabricado). esbuild EXIT 0; review LOCAL 0 críticos/0 altos.
1. **T3 — Wandson revisa o mapa de telas v0** (`WikiBrain/wiki/T3 — Mapa de Telas (Console Interno).md`) → Cowork constrói o **protótipo clicável** das 14 telas do MVP (React, dados fake). (Track T3)
2. **FASE 2 onda 2** — Cowork redige, Wandson aprova SQL, Cowork aplica (D5 v2): P-2 cutover `logAgentRun` + `agent_runs.tenant_id SET NOT NULL` · P-3 contract `user_agent_access` · P-4 `tenant_agent_config` · P-5 aposentar 3 grants órfãos de ex-membros · limpar 5 tenants seed. (Tracks T2 + T5 + T8)
3. **5.5** — consolidar docs de plano redundantes. (Track T2)

---

## 📌 Fatos canônicos (corrigem valores antigos em circulação)

- **Empresa:** Parauapebas-PA *(não Imperatriz-MA)*.
- **Equipe humana = 1 pessoa: Wandson.** Os agentes IA cobrem as demais funções da empresa. Wélida saiu (jun/2026); Eduardo saiu (jun/2026); Yasmin saiu (mai/2026 — branch `yasmin/dev` pode ser deletada).
- **VPS:** `187.127.25.24` *(a antiga `45.39.210.183` não é mais usada)*.
- **Supabase:** ref `czyanilrverorwenikqw` · tenant `consult` = `9079bd4d-4df7-4023-90fb-d79c8ba7e900`.
- **Engine de agente:** `@anthropic-ai/sdk` via **Messages API** *(não OpenClaw/Agent SDK como motor)*.
- **Trigger.dev:** v4 cloud · `proj_slexhoelcjwgbopmbzzr`.
- **Repo:** `deli-consult-delivery/consult-delivery` (privado) · **Domínio:** `app.consultdelivery.com.br`.

---

## 🔒 Decisões travadas (não rediscutir sem motivo novo)

- [x] Replicar o **paradigma** do EvoNexus, **não copiar o motor**.
- [x] Agente nativo via **Messages API** + tools de domínio + enforcement na nossa camada (RBAC), não no frontmatter.
- [x] **Recusa permanente:** OAuth-de-assinatura (circumvenção). Provedores só via API key.
- [x] **Hermes** = copiloto pessoal do CEO + referência MIT. **NÃO** é o motor da plataforma (seria mono-tenant).
- [x] **FASE 1:** escopo do *mapa* = tudo (CORE + operacional + ADAPTA). *Implementação* fatiada: CORE → operacional → ADAPTA.
- [x] Tabelas que a CD já tem: re-desenhar o schema-**alvo** onde precisa, via **migração não-destrutiva** (expand/contract). **Nunca DROP em produção.**
- [x] Toda mudança de schema = **migration versionada** em Git.
- [x] **DELI** = nome travado do orquestrador; título **COO digital** ✅ travado 2026-06-06 (RESTRUCTURE.md alinhado). Copy: usar **"oferta"**, nunca "promoção".
- [x] **D1 (runtime):** `@anthropic-ai/sdk` (não Agent SDK — não roda em Trigger.dev cloud) + camada multi-provider (Anthropic/Ollama/OpenRouter, BYO-key por tenant via Infisical). ✅ 2026-06-03 — ver `docs/evonexus-replica/DECISAO-001-runtime-provider-custo.md`.
- [x] **D2 (Trigger.dev):** v4 (`npx trigger.dev@4.4.6 deploy`), não v3. ✅ 2026-06-03.
- [x] **D3 (ambiente FASE 0):** FASE 0 roda na **VPS** (`187.127.25.24`) / `cd-evonexus-lab`, não do Windows. ✅ 2026-06-03.
- [x] **D4 (fork A vs B em `agents`): B** — catálogo global + habilitação por tenant via `tenant_agents`/`tenant_agent_config`. ✅ 2026-06-06 — evidência em `docs/evonexus-replica/FASE-1-mapeamento-multitenant.md` §3.1. **Cabeado em produção na onda 1 (2026-06-06).**
- [x] **D6 (direcionamento SaaS, aprovada pelo Wandson em 2026-06-07):** F1 = "Defesa Comercial iFood — modo copiloto" a R$147/loja/mês · carteira intocada · gate D+90 · anti-dispersão. Doc: `docs/estrategia/DIRECIONAMENTO-SAAS-2026-06.md` + `F1-BUILD-PLAN.md`. **F1 entregue (PR1–PR7) em 2026-06-07.** ⚠️ **REABERTA pelo Wandson em 2026-06-07** (mesmo dia, pós-entrega da F1): partir para a plataforma completa sem aguardar o gate D+90 — decisão consciente do fundador, alertado sobre a colisão com a regra anti-dispersão. Detalhe no Tracker (`WikiBrain/wiki/PLANO-MESTRE — Tracker.md`).
- [x] **D5 v2 (mandato Cowork, alterada pelo Wandson em 2026-06-06):** Cowork executa direto tudo que conseguir — incluindo **merge de PRs** e **aplicar migrations cujo SQL foi aprovado pelo Wandson** (sempre: SQL versionado antes · 1 arquivo por vez · validação com output bruto · parar no 1º erro · teste de isolamento quando tocar RLS). **Reservado ao Wandson:** aprovar o SQL antes da aplicação · `DROP`/destrutivo · mensagens a clientes (drafts) · reabrir decisões travadas / 🛑 CHECKPOINTS · credenciais e VPS. (v1 em 2026-06-06: tudo exceto merge/aplicação; v2 no mesmo dia liberou ambos.)

---

## 🧱 Regras duras (disciplina de execução)

- Output **bruto** sempre (SQL/JSON/commit) — resumo confiante não substitui evidência.
- Nunca confiar em "está pronto" sem validar com output real.
- Antes de mexer em branch da VPS: `git log` + `diff origin` (branches divergem de origin).
- Smoke test via endpoint HTTP, nunca INSERT SQL direto.
- Doc autoritativo / DB / código **vence** memória — e a divergência é registrada.
- **Edit tool falha em match multi-linha por CRLF** → para edições programáticas usar scripts `.cjs`.
- `gh pr merge` local pode falhar (worktree `main` travado) → verificar com `gh pr view N --json state,mergedAt`.
- **Não reusar branch que já entrou em main por squash** — gera conflito fantasma (caso #155). Branch nova por entrega.
- **Migration aplicada = 1 arquivo por vez + validação + teste de isolamento (RLS)** — padrão da onda 1.

---

## T1 — Plataforma CD (V1 → V3)

**V1 (~95%)** `(memória·confirmar contra repo)`
- [x] 1A–1F concluídas `(memória·confirmar)`
- [x] 1G — AgentsPage real + Notificações `(memória·confirmar)`
- [ ] 🔄 1E — DELI Realtime: finalizar triggers

**V2 (jun–jul/2026)**
- [ ] ⏳ Custom fields, automations, dashboards, CRM
- [ ] ⏳ Ativar SOFIA / LARA / CORA

**V3 (ago/2026+)**
- [ ] ⏳ Onboarding self-service, billing, white-label, marketplace de agentes

---

## T2 — EvoNexus-replica (multi-tenant)

### Status das fases

| Checkpoint | Gate | Status |
|------------|------|--------|
| Plano persistido | Este doc + ponteiro no CLAUDE.md | ✅ feito (2026-06-02) |
| Decisão D1/D2/D3 | Runtime + multi-provider + ambiente FASE 0 (`DECISAO-001`) | ✅ DECIDIDO (2026-06-03) |
| 🛑 CHECKPOINT 0 | Inventário técnico (FASE 0 na VPS) + sign-off | ✅ FASE 0 merged em main (#156) |
| 🛑 CHECKPOINT 1 | Mapeamento do checklist | ✅ GO dado (2026-06-06) — FASE 1 merged (#152; B decidido) |
| 🛑 CHECKPOINT 2 | Schema + migrations (SQL aprovado antes de aplicar) | ✅ **onda 1 APLICADA** (2026-06-06): SQL aprovado pelo Wandson (#161) → aplicada pelo Cowork (D5 v2) → validações 15/0/0 + isolamento (intruso 0/0/0). Ondas seguintes reabrem o gate. |
| 🛑 CHECKPOINT 3 | Plano faseado + checklist de completude | — |
| FASE 4 | Build incremental + sign-off final | — |

### Próximos itens

- [x] FASE 0 ✅ (#156) · FASE 1 ✅ (#152) · CHECKPOINTs 0–1 ✅
- [x] **FASE 2 onda 1 ✅ APLICADA** (2026-06-06): `tenant_agents` populado (15) · 4 brechas RLS corrigidas · backfill 383 runs · `user_agent_access` expandida (P-1 `main`→`deli`). Migrations `20260607_001..005` + `docs/evonexus-replica/FASE-2-onda1-plano.md`.
- [ ] 👉 **FASE 2 onda 2** — P-2 cutover `logAgentRun` + NOT NULL · P-3 contract `user_agent_access` · P-4 `tenant_agent_config` · P-5 grants órfãos · tenants seed
- [ ] ⏳ FASE 2 — desenho das 5 peças do framework + tabelas novas (skills, goals, tickets, templates, shares)

### Ground truth — o que já existe na CD (estender, não recriar)

Confirmado no schema real, nas migrations e na extração ao vivo (2026-06-06 pós-onda-1: `agents` 15 · `agent_runs` 1686 todos com tenant · `tenant_agents` 15 · `lojas` 1173 · `customers` 1169).

**Tabela `agents`:** id text PK (slug) · `tenant_id` nullable (NULL = global) · RLS pós-onda-1: `agents_select_gated` (custom do tenant OU habilitado via `tenant_agents`) + escrita só admin/custom. Catálogo global gerido por migrations/service_role. ⚠️ Inserir o agent ANTES de `logAgentRun` (FK).

**Outras tabelas a reusar:**

| Tabela | Estado | Papel |
|--------|--------|-------|
| `agent_runs` | 1686 linhas, 100% com tenant ✅ | Log unificado (Atividade + Custos) |
| `tenant_agents` | **15 linhas ✅ (cabeada onda 1)** | Habilitação por tenant (D4/B) |
| `tenant_agent_config` | 0 linhas (P-4 onda 2) | Config/modo por tenant+agente |
| `agent_memories` | 0 linhas, `tenant_id NOT NULL` ✅ | Memória por agente/tenant — ativar |
| `agent_drafts` | 19 cols, 0 linhas | Fluxo de aprovação já existe |
| `roles` / `role_permissions` / `user_roles` | 9 / 112 / 4 | RBAC parcial |
| `user_agent_access` | 7 linhas, `tenant_id`+`agent_id` FK ✅ (contract na onda 2) | Acesso por usuário+agente |
| `audit_log` | ✅ | Auditoria — estender ações |
| `customers` | 1169 · só `customers_member_all` ✅ | ⚠️ usar `customers`, `clientes` não existe |
| `tenants`, `tenant_members`, `lojas` (+filhas) | ✅ | Base do isolamento |
| pgvector · Storage `public` · `internal_channels` | ✅ | RAG · Workspace · Chat |

### Checklist Mestre — feature surface completo

> Mapeamento completo linha a linha: `docs/evonexus-replica/FASE-1-mapeamento-multitenant.md` §2.1 + `FASE-0-inventario-evonexus.md` §6. (Tabela detalhada nas versões ≤ v2.4 deste doc — histórico git — e nos docs da FASE 0/1; removida daqui para evitar tripla manutenção.)

Resumo de categorias: **CORE** dashboard/agentes/oracle→DELI/skills/atividade/metas/tickets/templates/workspace/links/memória/custos/chat/create-* · **NATIVO** auth/RBAC/auditoria/pgvector · **ADAPTA** rotinas/tarefas/gatilhos/heartbeats/config/provedores/integrações/serviços/backups · **DEPOIS** MemPalace/MCP/terminal/plugins/docs/sistemas.

### Camada de Framework (5 peças, multi-tenant)

1. **Registro de agentes** — `agents` + `tenant_agents` ✅ cabeado (onda 1).
2. **Sistema de skills** — tabela `skills` nova (CHECKPOINT 2 / onda futura).
3. **Orquestrador (Oracle da CD)** — recebe `tenant_id`, carrega agente, monta contexto, executa via `@anthropic-ai/sdk`, loga em `agent_runs`.
4. **Memória por tenant** — reusar `agent_memories` (NOT NULL ✅); portar KB iFood.
5. **Fluxo create-*** — endpoints/UI por tenant.

### Fases detalhadas

- **FASE 0** ✅ MERGED (#156). **CHECKPOINT 0 ✅.**
- **FASE 1** ✅ MERGED (#152). **CHECKPOINT 1 ✅ go 2026-06-06.**
- **FASE 2** — 🔄 **onda 1 ✅ APLICADA** (segurança + cabear B). Próximas ondas: contract/cutover (onda 2) → tabelas novas do framework. **🛑 CHECKPOINT 2 reabre por onda** (SQL aprovado antes de aplicar).
- **FASE 3** — plano faseado + checklist de completude. **🛑 CHECKPOINT 3.**
- **FASE 4** — build incremental; isolamento A-não-vê-B com tenant real; sign-off final.

---

## T3 — Visão Visual-First do MVP (telas novas)

- [x] **(a) Mapa de telas** — v0 em main (32 telas, MVP = 14) — 👉 aguarda revisão do Wandson
- [ ] ⏳ **(b) Protótipo clicável** (React, dados fake) — Cowork constrói após revisão do mapa
- [ ] ⏳ Travar inventário de telas + fluxos → só então código/DB
- [x] **Série LEVA ✅ (sessão 30, 2026-06-09):** todas as telas de referência do Console v2 saíram do mock e leem do Supabase. LEVA 1 (CRM/customers) · LEVA 2 (`tenant_provedores`/`tenant_sistemas`/`crm_notas`, migration `20260609_003`) · LEVA 3 (`tenant_integracoes`, migration `20260609_004` aplicada + RLS E2E membro=4/não-membro=0 + seed 4 reais, PR #275 `f2dde20`). Padrão: tabela `tenant_id NOT NULL` + RLS SELECT-only via `is_member_of`, escrita por service_role (equipe CD), zero segredo no banco.

---

## T4 — Hermes (copiloto CEO)

- [x] **3A — instalar isolado na VPS** ✅ (kimi-k2.6 · Docker · Telegram allowlist Wandson)
- [x] 🟢 **3B — acesso à CD via admin MCP — GO-LIVE VALIDADO (2026-06-10, cont. 13):** os 2 comandos rodaram, **leitura ponta-a-ponta funciona**. `live-smoke OK` com chave real (219 chars) — 6 read tools responderam contra o banco + `audit_log` gravou (`id 101–104`, `ceo_agent`) · `hermes mcp test cd-admin` → `✓ Connected (244ms)` · 7 tools (gateway regravado com a chave real). Desbloqueios: `npm install` faltava (passo 0 — `node_modules` nunca instalado; PR #295) · placeholder de 25 chars · linhas-em-branco quebrando o bash. **RUNTIME FECHADO (2026-06-10, cont. 14):** copiloto Hermes (Telegram @DeliConsultBot) → gateway → MCP `cd-admin` → banco real, ponta-a-ponta. Wandson mandou "qual o semáforo da CD agora?" → bot exibiu badge `mcp_cd_admin_cd_status` + respondeu **🟢 VERDE** (Bridge online + Supabase acessível) → gravou `audit_log id 106` (`ceo_agent`, `mcp:cd_status`, 04:34:33 UTC, casando com o "Verificado em" do bot). Dois desbloqueios: **(a)** gateway rodava desde 21:36 (Jun 9) com config anterior ao `cd-admin` → **faltava `systemctl restart hermes-gateway`** pra carregar o MCP (1ª resposta foi "não conheço Consult Delivery" — bot nem chamou a tool); **(b)** o `hermes mcp add` gravou um **placeholder literal** (`COLE_A…`) no `SUPABASE_SERVICE_KEY` do config → tool conectava mas dava `401 Invalid API key` (handshake ≠ runtime, de novo). **Virada de segurança:** em vez de rolar o JWT secret (rotação da `service_role` legada → regenera `anon` → derruba frontend+Bridge+Trigger+Edge), criamos uma **chave nova dedicada `sb_secret_hermes-admin-mcp`** (sistema novo de API keys do Supabase, já habilitado no projeto — havia `sb_publishable_…` ativa) — fresca, nunca vazou, **revogável sozinha**. Injetada no config via `admin-mcp/scripts/fix-hermes-service-key.sh` (lê a chave por `read -s`, nunca pelo chat) + restart. **Pendência sem pressa:** ⏳ **migração das chaves legadas** — migrar frontend→`publishable` e Bridge/Trigger/Edge→`sb_secret_` e então **desabilitar as keys legadas** pra invalidar de vez a `service_role` que vazou no chat (cont. 12-13). Histórico do destrave: **GATE 0 concluído + SQL aplicado + admin MCP construído (2026-06-09)** → [x] ✅ **admin MCP desenhado** (`docs/infra/admin-mcp-design.md` — `ceo_agent` escopado, read amplo + write só propõe-e-aprova via drafts, auditado) · [x] ✅ **runbook do Wandson** (`docs/infra/RUNBOOK-WANDSON.md`) · [x] ✅ **escopo `ceo_agent` = todos os tenants** (Wandson) + achado cross-tenant (§2.1: RBAC per-tenant, `ceo_agent` cross-tenant → Opção A) · [x] ✅ **SQL do `ceo_agent` APLICADO** (`supabase/migrations/20260609_001_ceo_agent_hermes.sql`, Opção A: aditivo — `agent_drafts.origin` com CHECK + índice parcial; Wandson deu `ok`; **teste de isolamento RLS** membro=3/intruso=0) · [x] ✅ **código do admin MCP construído e testado offline** (`admin-mcp/`: 6 tools de leitura cross-tenant + `cd_propor_draft` única escrita, sempre `pending`/`origin='hermes'`; enforcement estrutural — sem tool de aprovar/enviar/executar; config fail-closed Infisical; auditoria total; `npm run smoke` 5/5 + `npm run test:integration` handshake MCP real → 7 tools) · [x] ✅ **usuário `claudedev` criado** (Wandson; build verde, sem `bypassPermissions`) · [x] ✅ **"§3 SQL do `ceo_agent`" DISSOLVIDO (2026-06-10, cont. 11):** varredura do schema real confirmou que **não há migration a aplicar** sob a Opção A (enforcement no MCP, não no banco) — único entregável é o config `CD_AUDIT_TENANT_ID=9079bd4d-4df7-4023-90fb-d79c8ba7e900`; RUNBOOK §3 reescrito + correção "rode como root, não claudedev" · ⏳ **falta só o Wandson rodar (como root) os 2 comandos** que carregam o `service_role`: `hermes mcp add cd-admin --env …` + `npm run live-smoke` → leitura liberada (pré-req: `service_role` dedicado do cd-admin no Infisical; ver `admin-mcp/README.md` + `RUNBOOK-WANDSON.md`).

- [ ] ▶ **3C — AI-First Hermes-First (plano-mestre APROVADO 2026-06-28):** montar a org de agentes (DELI + 12 profiles) DENTRO do Hermes Agent (Nous) primeiro, integrar à plataforma CD depois. **Supabase = fonte única do estado do loop** (Kanban do Hermes = cache); roteamento como **dado**; ações via MCP→Bridge; verificação por agente revisor (grounding + efeito real); fluxo novo "CEO autoriza demanda do cliente". Doc: `docs/ai-first/BLUEPRINT-AI-FIRST.md` **v2**. **GATE 0 segurança P0 (BLOQUEANTE) em andamento** — fatia 1 ✅: `bridge-server/index.js` fail-closed + constant-time (`safeTokenEqual`) + boot check (`node --check` OK). Próximo autônomo: proteger writes VendaERP, `erp_confirmar` estrutural, authz por tenant server-side, audit fail-closed, `hermes/` config-como-código, despachador por `tenant_agents`. Reservado ao Wandson: rotacionar `VENDAERP_TOKEN` (vazado), root→`claudedev`, sandbox docker, ligar `cd-admin` live.

---

## T5 — Segurança / GATE 0

- [x] fail2ban ✅ · SSH key-only ✅ · Deploy key dedicada ✅
- [x] **4 brechas RLS corrigidas EM PRODUÇÃO (onda 1, 2026-06-06):** `customers_auth_all` · `user_agent_access_manage_admin` sem escopo · `agents_read_all` · escrita em agents globais por membro. Isolamento provado por impersonação (intruso = 0/0/0).
- [ ] ⏳ **Rotação de credenciais** (adiado): 4 PATs GitHub · `DASHBOARD_API_TOKEN` · token Telegram · limpar cópias na VPS
- [ ] ⏳ Hardening: gateway root→usuário · remover `claude-debug` key · fail2ban `bantime.increment`

---

## T6 — Agentes IA

**Produção** `(confirmar status atual)`
- [x] BomDia · Encerramento · chat ao vivo
- [ ] 🔄 DELI (orquestrador) · [ ] CORA — confirmar · [ ] Analista iFood / loja-gpt — confirmar

**Épico "Dashboard iFood — extração máxima das planilhas"** (multi-PR, plano `/root/.claude/plans/dynamic-swimming-zebra.md`; 7 fases, cada uma 1 PR deployável sozinho). Objetivo do Wandson: extrair o máximo das planilhas do iFood pra um dashboard mais completo (consultor decide + automação + lançar demandas + acompanhamento) + filtro diário/semanal/mensal/custom. Restrição honesta (anti-padrão P1): iFood entrega Vendas/Cardápio/Conciliação agregados — diário real só em Operação/Cancelamentos/Logística.
- [x] **FASE 0 — fundação temporal** ✅ (sessão 45): Migration A `20260615_001_radar_temporal.sql` (colunas `granularidade`+`data_ref` em `radar_metricas` + índice de dedup temporal + backfill; aditiva/idempotente/reversível, APLICADA) + parser `processar-fontes.ts` preenche o grão no insert (deploy Trigger.dev `20260614.12`); verificado E2E (2 fontes de meses diferentes → 2 `data_ref` distintos, backfill 100%).
- [x] **FASE 1 — refactor do dedup → helper único `lerMetricas`** ✅ (comportamento-neutro, CAMINHO CRÍTICO): 2 helpers (`trigger/_shared/radar-metricas.ts` + `src/lib/radar-metricas.js`); os 4 call-sites (`RadarReal.jsx`, `agente-analise.ts`, `analise-loja/processar.ts`, `diagnostico-semanal.ts`) trocaram o bloco inline pela chamada **sem `periodo`** → diff de comportamento NULO (verificado por review LOCAL: "all four call-sites reproduce the prior inline behavior exactly"). + guarda anti-vazamento `selectEfetivo` (garante `loja_id` no select quando o filtro client-side `incluirSemLoja` roda — no-op nos call-sites atuais, blinda a Fase 4). Deploy Trigger.dev. · [x] FASE 2 — enriquecer parser (sessão 49, validada em prod) · [x] FASE 3 — cruzamentos no dashboard (sessão 50, só frontend) · [x] FASE 4 — filtro temporal UI (sessão 51, só frontend) · [x] FASE 5 — série diária `radar_series` (sessão 53, Operação + Migration B + deploy) · [x] FASE 6 — demandas rascunho→aprovação em `tarefas_loja` (sessão 55, índice único `uq_tarefa_ia_origem_ativa` + painel "Ações recomendadas") · **🏁 ÉPICO COMPLETO (Fases 0–6); Balde 3 deferido (precisa `.xlsx` real)**
- [x] **FASE 2 — enriquecer o parser (Balde 1, aditivo via EAV)** ✅ EM PROD E VALIDADA (sessão 49, #349, deploy `20260614.33`): `trigger/radar/processar-fontes.ts` ganhou **7 famílias de métricas novas** sem DDL (EAV absorve) e com o guard `reqCol` fail-loud preservado — **Conciliação:** `conciliacao_repasse_liquido` (entrada−taxas+subsídios) + `conciliacao_lancamentos.metadata.por_tipo` (estorno/antecipação/ajuste); **Cancelamentos:** `cancelamentos_contestaveis_qtd`; **Cardápio:** `cardapio_itens_sem_giro` (cauda-longa de baixo giro); **Operação:** `operacao_cancelamentos_super_qtd` (contagem absoluta) + `operacao_metas` (Meta REAL da planilha, com `valor_texto` — não mais hardcoded); **Logística:** `logistica_pedidos.metadata.por_turno`+`por_servico` (iFood vs própria). **Saga do `data_ref` da Operação:** o fix da sessão 48 (`9216fd2`) ancorava `operacao.data_ref` via `parsePeriodo`, mas o regex ESTRITO (`DD/MM/YYYY - DD/MM/YYYY`) **não casava** o label real `"Diário (09/05 a 07/06)"` (formato curto, sem ano) → as 14 métricas `operacao_*` seguiam com `data_ref` = hoje. **Corrigido na sessão 49 (`1f728a5`):** `parsePeriodo` ganhou 2º ramo p/ o formato curto `DD/MM[/YYYY] <a/à/até/-/–/—> DD/MM[/YYYY]` + inferência do ano ausente (fim no futuro → ano anterior; início > fim → virada de ano), sem fabricar dado; o ramo estrito casa primeiro (Vendas/Cardápio intactos). Diff +44/−3 (sessão 48) + parser do período (sessão 49); commits `b398509`+`9216fd2`+`1f728a5`. **Merge-first** p/ driblar a corrida deploy-vs-reset (o `git reset --hard origin/main` da rotina de bridge-restart clobberou um checkout detached mid-build → o deploy `20260614.21` saiu SEM a Fase 2; provado por SQL bruto). **✅ Gate validado por output bruto em prod (reprocesso 17:40):** métricas novas presentes (operação 14, cardápio 6, vendas 4), nenhuma antiga sumiu, `operacao.data_ref=2026-06-07` (`periodo_inicio=2026-05-09`/`periodo_fim=2026-06-07`, granularidade=`periodo`), sanity `conciliacao_repasse_liquido=8427.01`/`conciliacao_lancamentos=3094`/`cancelamentos_contestaveis_qtd=3`/`logistica_pedidos=445` (metadata íntegra). **Vendas (horário de pico/formas de pagamento/melhor dia) ficou para depois** — exige inspecionar `.xlsx` real (anti-padrão P1, Balde 3 deferido).
  - **Pré-requisitos da FASE 5 (achados da revisão LOCAL da Fase 0, deferidos):** ao migrar o dedup p/ `data_ref desc`, (1) alinhar timezone do `data_ref` — o parser grava `new Date().toISOString()` (UTC) e o backfill usou `created_at::date` (DB Supabase é UTC por default → batem hoje, mas confirmar antes de ordenar por `data_ref`); (2) documentar/validar que `loja_id is null` forma grupo de dedup próprio no índice `radar_metricas_dedup_idx` (métricas tenant-wide não deduplicam contra métricas por-loja).
  - **A resolver na FASE 4 (achados latentes da revisão LOCAL da Fase 1 — hoje inertes, nenhum call-site passa `periodo`):** (a) no modo legado `incluirSemLoja`, o filtro de `data_ref` no servidor (`.gte/.lte`) roda ANTES do filtro client-side de loja → linhas tenant-wide com `data_ref` NULL ficam invisíveis quando há período; ao ativar período por loja, mover o filtro de loja para o servidor ou tratar `data_ref` NULL; (b) o dedup pega a métrica tenant-wide mais recente sobre a específica da loja quando ambas existem no modo `incluirSemLoja` (preserva o legado, mas revisar na semântica de período); (c) `limit(400)` trunca em janela ampla — o helper filtra `data_ref` no servidor antes do limit, mas confirmar o teto ao habilitar histórico longo.
- [x] **FASE 3 — dashboard completo + cruzamentos (Balde 2, só frontend)** ✅ ENTREGUE (sessão 50, só `src/console/RadarReal.jsx` — sem migration, sem deploy Trigger.dev): nova grid **"Saúde financeira & cruzamentos — calculados a partir dos relatórios desta loja"** com **7 KPIs derivados client-side** das chaves REAIS do EAV (anti-padrão P1 respeitado — zero chute de coluna): receita/repasse líquido (`conciliacao_repasse_liquido` autoritativo, fallback `entrada+taxas+subsídios` — taxas/subsídios são NEGATIVOS → soma), **carga total iFood %** (`(|taxas|+|subsídios|)/|entrada|`), **taxa de cancelamento %** (`cancQtd/pedidosBase`, base = pedidos∥opPedidos∥logística), **R$ perdido em cancelamentos** (`operacao_valor_cancelado`), **visitas não convertidas** (`funil_visitas − funil_concluidos`), **avaliação vs piso Super 4,5** (gap rotulado, regra de negócio iFood — NÃO dado fabricado), **negociações** (`negociacoes_qtd`+`negociacoes_perda_parcial`). Cada KPI guardado por `!= null` → **some quando falta a fonte, nunca zera** (o helper `val(k)` devolve 0 para zeros reais, null só para métrica ausente). + sinais enriquecidos: cancelamentos (motivo top `cancelamentos_motivo_top` + contestáveis + R$ em jogo que "a Defesa contesta") e chamados (`operacao_chamados`). esbuild EXIT 0 (15.9kb). Reusa `<Kpi>` + `.cv2-kpis`/`.cv2-sub` (Console v2). Aceite atingido: loja com relatórios completos mostra os cruzamentos; loja sem um relatório não quebra.
- [x] **FASE 4 — filtro temporal universal (UI + ramo `periodo` do helper, só frontend)** ✅ ENTREGUE (sessão 51, só `src/console/RadarReal.jsx` — sem migration, sem deploy Trigger.dev; o ramo `periodo` do `lerMetricas` já existia desde a Fase 1, dormente). Card de filtros de 2 linhas (loja preservada na linha 1; período na linha 2) com botões-janela `Tudo · Dia · Semana · Mês · Personalizado` (reusa `.cv2-btn`/`.cv2-btn.sec` = vermelho-selecionado/cinza-não) + date-range `<input type=date>` no modo Personalizado. `calcPeriodo(janela,ini,fim)` traduz a janela em `{inicio,fim}` (presets = janelas ROLANTES relativas a hoje: dia=hoje, semana=−6d, mês=−29d; Personalizado = meses-calendário absolutos) e o `carregar` passa `periodo` ao `lerMetricas` → filtra `data_ref` no servidor (`.gte/.lte`) + dedup por `data_ref desc` (o snapshot certo do período, não o último upload). **Honestidade sobre o grão (anti-padrão P1):** com período ativo, aviso fixo de que o iFood entrega relatórios agregados por período (o filtro recorta pela `data_ref` = fim do período) + "mostrando dados até DD/MM/AAAA" (`dataRefMax`); janela sem relatório → banner honesto "Nenhum relatório **do iFood** nesta janela de período" (nunca tela em branco, nunca gráfico fabricado). esbuild EXIT 0 (852.6kb). Aceite atingido: "fevereiro mostra fevereiro" via Personalizado (não o último upload); "custom 01–15/03" filtra por `data_ref`; granularidade não-suportada → snapshot do período com aviso honesto. Review LOCAL (cd-lens): 0 críticos / 0 altos / 1 médio (copy do banner, corrigido) / 2 baixos (sem ação). **Série diária de verdade (Operação/Cancelamentos) = Fase 5.**
- [x] **FASE 5 — série diária de verdade (`radar_series`) onde há grão fino** ✅ ENTREGUE (sessão 53; Migration B + parser Trigger.dev + frontend). **Por quê:** o parser já calcula o vetor por dia da Operação (`diaCols.map`) e hoje colapsa/descarta — persistir o grão dá o gráfico "diário de verdade" que o filtro temporal universal (Fase 4) não consegue para fontes agregadas. **Migration B `supabase/migrations/20260620_001_radar_series.sql` (APLICADA, `{"success":true}`):** tabela `radar_series` colunar — 1 linha/métrica/dia (`tenant_id NOT NULL refs tenants`, `loja_id`, `fonte_id`, `metrica`, `dia date`, `valor numeric`, `metadata jsonb`, `created_at`); índices `(tenant_id,loja_id,metrica,dia)` + `(fonte_id)`; RLS habilitado, `radar_series_select FOR SELECT USING (is_member_of(tenant_id))`, **sem insert policy por design** (escrita via cron service role bypassa RLS, igual `radar_metricas`); aditiva/idempotente/reversível. **Teste de isolamento RLS provado** (execução real): tenant membro vê / tenant estranho 0 linhas. **Parser (`trigger/radar/processar-fontes.ts`, ramo Operação):** nova `diasDaSerie(headersDia, periodo)` ancora cada coluna-dia `DD/MM` ao ISO **por proximidade a `[ini,fim]`** (corrige colunas fora de ordem cronológica + virada de ano; coluna fora do período → `null`); persiste 6 métricas espelhando os agregados → invariante **Σ série == KPI agregado** (`operacao_pedidos_totais`, `operacao_atrasados_5min`, `operacao_cancelamentos_super_qtd`, `operacao_avaliacoes_qtd`, `operacao_chamados`, `operacao_valor_cancelado`); idempotência **DELETE-por-`fonte_id` em `radar_series`** antes do insert (espelha `radar_metricas`). **Frontend:** helper novo `src/lib/radar-series.js` (`lerSeries` filtra tenant/loja/métrica/janela no servidor; `agregar` faz date_trunc no cliente dia/semana/mês com dedup por dia mais-recente); componente `SerieDiaria` (barras CSS, **sem dependência nova**) no `RadarReal.jsx`, só dentro de `temOperacao` e com série não-vazia. **Honestidade (anti-padrão P1):** **Vendas/Cardápio/Conciliação continuam SEM série** (iFood entrega agregado — fabricar diário = inventar dado); Cancelamentos/Logística ficam para iterações futuras. **Review LOCAL (cd-lens):** 3 bugs corrigidos (datas fora de ordem corrompiam ISO; ano-errado-quando-período-null; drift de centavos no `valor_cancelado`); 2 baixos registrados sem ação. **Provas (output bruto):** `tsc --noEmit` EXIT 0 · `vite build` EXIT 0 · 6/6 testes lógicos do `diasDaSerie` · Migration `{"success":true}` · RLS isolamento real. Aceite atingido: relatório Operação de N dias → N linhas/métrica em `radar_series`; semanal soma certo; Vendas sem série. **Próxima = Fase 6.**
- [x] **FASE 6 — demandas: rascunho automático + aprovação (em `tarefas_loja`)** ✅ ENTREGUE (sessão 55; parser Trigger.dev + frontend + migration de idempotência) — **ÚLTIMA FASE, ÉPICO COMPLETO.** **Por quê:** fechar o ciclo "consultor decide → lança demanda → acompanha" sem que a IA crie tarefa "valendo" sem o clique do Wandson (decisão travada 2026-06-14: *rascunho automático + aprovação*). **(a) Geração (`trigger/radar/diagnostico-semanal.ts`, +195/−3):** `montarRascunhos(map)` deriva até **8 sinais** das métricas EAV da loja — `carga_ifood` (operacao/estrutural, `cargaPct>40`), `conversao_baixa` (marketing/estrutural, `conv<25`), `subsidios` (marketing/estrutural, `|subsídios|>0`), `cancelamentos` (operacao, quick_win se por atraso senão estrutural), `op_atrasos` (operacao/estrutural, `>5%`), `op_online` (operacao/quick_win, `<90%`), `op_canc_super` (operacao/estrutural, `>1%`), `op_chamados` (suporte/quick_win, `>0`) — cada um com `origem`/`bloco`/`prioridade`/`metrica`. `gerarRascunhosTarefas(sb,tenantId)` insere em `tarefas_loja` com `status='rascunho'` (default), `criado_por_ia=true`, NOT NULL preenchidos (`loja_id`/`bloco`/`titulo`/`situacao`/`o_que_sera_feito`), `metadata={origem,fonte,metrica}`; **dedup SELECT-then-filter** por `metadata.origem` contra origens com tarefa IA aberta (não-terminal). Contabilizado no audit/log (`rascunhosCriados`). **(b) Backstop de idempotência — Migration `supabase/migrations/20260620_002_tarefa_ia_origem_unique.sql` (APLICADA):** índice único parcial `uq_tarefa_ia_origem_ativa ON tarefas_loja (loja_id, (metadata->>'origem')) WHERE criado_por_ia AND status NOT IN ('concluida','cancelada','rejeitada')` — garante ≤1 rascunho IA ATIVO por (loja, origem) mesmo em corrida concorrente que o SELECT-then-filter não cobre; origens terminais ficam fora do índice (novo ciclo recria após encerrar); insert trata `23505` como no-op. Aditiva/reversível; verificado antes (0 pares duplicados, 0 tarefas IA hoje). **(c) UI (`src/console/RadarReal.jsx`, +85; `src/lib/api.js`, +44):** painel **"Ações recomendadas"** lista os rascunhos IA da loja (`listTarefasIA`) com botões **Aprovar**/**Rejeitar** (`aprovarTarefa`/`rejeitarTarefa` fazem update guardado por `.eq('status','rascunho')` → passa por RLS; transição inválida lança "Tarefa não encontrada ou já processada") + acompanhamento por status (rascunho vs. já movidas). **Review LOCAL (cd-lens xhigh):** 4/5 achados tratados (dívida aceita e documentada do `cargaPct`). **Provas (output bruto):** `tsc --noEmit` EXIT 0 · `vite build` EXIT 0 · Migration `20260620_002` aplicada. Aceite atingido (verbatim do plano): "rodar o diagnóstico semanal → rascunhos aparecem na loja certa; aprovar move o status e o card sai de 'rascunho'; nada vira tarefa 'valendo' sem o clique do Wandson." **🏁 Épico Dashboard iFood — Fases 0–6 entregues. Balde 3 DEFERIDO** (quebra fina por colunas de Vendas/Itens/negociações — gated por P1: precisa o Wandson subir um `.xlsx` real antes, não chutar layout).

**MIA:** spec completa (`docs/mia/MIA-PLANO-COMPLETO.md`) · ⏳ implementação
**Planejados:** LARA (spec feita) · SOFIA (ICP definido) · BRENO off-hours · MAX · VERA
- [x] ✅ **Agente `avaliacoes` (sessão 47, 2026-06-14, MERGEADO + EM PRODUÇÃO)** — responde avaliações do iFood das lojas de consultoria. Aba Console v2 "Avaliações" (`src/console/Avaliacoes.jsx`) + 3 endpoints Bridge (`routes/avaliacoes.js`: `gerar`/`enviar-grupo`/`sugerir-tom`, claude-runner `claude-sonnet-4-6`) + migration `20260614_001_avaliacoes.sql` (`avaliacoes`/`avaliacoes_loja_config`, **aplicada + RLS validada**) + registro em `agents`/`tenant_agents`. Sem API do iFood (consultor cola/copia manual). Regra de logística (iFood logística não responde entrega) · resposta ≤300 chars + insights de consultoria · envio ao grupo de WhatsApp 1 a 1 (Evolution). Sem deploy Trigger.dev (geração no Bridge). **✅ PR #344 squash-mergeado em main (`8624c7d`); QA Pages PASSOU** (bundle `/assets/index-jDyM1iaN.js`, 7 strings exclusivas da feature). **GATE 0 — DESTRAVADO POR UI (sessão 50, 2026-06-14):** o painel **"Gerenciar lojas da consultoria"** dentro da própria aba Avaliações deixa o Wandson, em massa e sem SQL, definir a logística por loja (`avaliacoes_loja_config`: entrega própria vs logística iFood, via `setLojaLogistica` — upsert só-logística, preserva `tom`) e podar a lista ativa (`lojas.is_consultoria_ativa`, reversível, via `setLojaConsultoriaAtiva`); 6 pares duplicados **sinalizados** (badge), NÃO auto-deletados (DELETE admin-only). RLS conferida (`lojas_update_tenant` libera UPDATE ao membro; `avaliacoes_loja_config` tenant-gated) → **sem migration nova**. Baseline `entrega_propria` p/ as 38 lojas ativas já em prod via `20260614_002_avaliacoes_config_seed_gate0.sql` (#348, 100% cobertura). Entregue em 2 arquivos (`src/lib/api.js` +3 funções, `src/console/Avaliacoes.jsx` componente `LinhaGestao` + painel) — `npm run build` ✓. **Fora de escopo (v2):** cadência agendada ter/sex, parser de WhatsApp de entrada, leitura/postagem automática no iFood.

---

## T7 — Feature PILOTO

- [x] Ondas 01+02 merged · [x] ✅ **Onda 03 (Loja-GPT) EM PROD desde 2026-05-22** (verificado 2026-06-09: tabelas `loja_gpt_conversations`/`loja_gpt_messages`, 9 conv/16 msg; task `loja-gpt-responder`; 5 endpoints Bridge; `TabIaEspecialista`; ver `docs/piloto/PILOTO-03-LOJA-GPT.md`) · [ ] 🔄 Onda 04 (WhatsApp + Loom) — **iniciada**: [x] Tarefa 7 ✅ parser `parseRespostaCliente` em prod no `main` (`c5f3afc`, 18 testes verdes, verificado 2026-06-09); resto bloqueado por Wandson (Evolution + cliente real + migrations gated) → `docs/infra/RUNBOOK-WANDSON.md`

---

## T8 — Infra / CI-CD / Manutenção

- [ ] ⚠️ CI/CD: auto-deploy de `main` sobrescreve deploys manuais · [ ] ⚠️ Branches VPS divergem de origin
- [ ] ⏳ Limpar 5 tenants seed (entra na onda 2) · [ ] ⏳ `is_active` em `tenants` · [ ] ⏳ Deletar `yasmin/dev`
- [x] ✅ **Onboardar primeiro cliente real — FEITO (sessão 92, PR #508, SHA `0a803ac`).** Karina Doceria provisionada: tenant `e9fdaa66` + 3 módulos (`csat`,`nps`,`visao`) + admin via service-role + RBAC semeado (`seed_rbac_system_roles` + `user_roles`). 4 defeitos corrigidos: RBAC (CSAT/NPS sem "Acesso negado"), `VisaoGeralAvaliacao` (KPIs próprios), branding (logo CD fixa, sem "CONSOLE · BETA"), runbook atualizado com Passo 3c. **⚠️ Pendente do Wandson:** smoke visual no browser como admin do cliente (CSAT + NPS + Visão Geral sem "Acesso negado").

---

## T9 — Negócio (contexto, não-build)

- Roadmap 90 dias: **S1 Fundação · S2 Combustível · S3 Pivotagem**
- Metas: reduzir churn (~33% mensal) · migrar mix pró-automação IA · rumo a SaaS multi-tenant
- Tiers: Light R$500 · Performance R$500+12% · Enterprise R$1.200 · Automação IA: R$2.500 setup + R$1.500/mês

---

## T10 — Agente GESTOR (Consultor de iFood IA)

Plano completo: `docs/gestor/plano-gestor.md`. Aprovado 2026-07-02 — agente IA para as rotinas de consultoria de iFood (análise diária, respostas a avaliações, contestação de cancelamento, gestão de cardápio), supervisionado via draft→aprovação, com identidade dentro do Hermes (Fase B) e execução via task Trigger.dev/Bridge (Fase A).

- [x] **F0 — Fundação** ✅ MERGEADA (PR #681): migrations `20260702_001/002/003` (agente `gestor` no catálogo + `agent_chat_messages` + `ifood_portal_nome`/seed 16 lojas) aplicadas no banco + rota Bridge `/api/portal-worker/run` + `garantirLoja` no `ifood-portal-worker`.
- [x] **F1b — Chat + prep Hermes** ✅ MERGEADA (PRs #683/#685/#686): `trigger/gestor/conversa.ts` (cascata Ollama Cloud/kimi → OpenRouter → Anthropic, com tools + web search) + perfil Hermes fase B (`hermes/profiles/gestor/`).
- [x] **F1 — parcial (relatório semanal + esqueleto de coleta)** ✅ MERGEADA (PR #689): `trigger/_shared/telegram.ts` compartilhado + `trigger/gestor/relatorio-semanal.ts` + `trigger/gestor/coleta-diaria.ts` protegido pela trava `GESTOR_COLETA_ATIVA` (desligada por padrão). Falta ativar a trava após o gate.
- [x] **F2 — parcial (rota de aprovação de drafts)** ✅ MERGEADA (PR #690): `bridge-server/routes/gestor-aprovacao.js` (whatsapp + portal_ifood). Falta escalar geração/publicação real nas 16 lojas.
- [x] **Dashboard v1 por loja** ✅ MERGEADA (PR #688): `src/console/GestorDashboard.jsx` (KPIs + reputação + diário + sugestões).
- [x] **Hardening** ✅ MERGEADA (PR #691): secrets hardcoded (anon key + Evolution key) removidos de `PainelAvaliacoesConsultor.jsx`.
- [ ] F1 — Coleta diária completa (cron + alertas ativos, hoje atrás da trava `GESTOR_COLETA_ATIVA`)
- [ ] F2 — Avaliações em escala completa (16 lojas, publicação real ao vivo)
- [ ] F3 — Contestação de cancelamento
- [ ] F4 — Gestão de cardápio (via API oficial)

**Pendências de gate (Wandson) antes de ativar em escala:** login único consolidado do Portal iFood (email/senha + 2FA das 16 lojas) · probe do seletor do modal "Escolher loja" no viewer do `ifood-browser` + validar `garantirLoja` entre 2 lojas reais · deploy do Bridge na VPS (`pm2 restart bridge-server` — rotas `portal-worker.js`/`gestor-aprovacao.js` novas) · rotação da chave Evolution que estava hardcoded (tratar como comprometida).

---

### Histórico de atualizações

- 2026-07-05 (v2.24) — **LEVA 3 — gates do Wandson executados, todos com aprovação explícita dele hoje de tarde.** **#745** listagens de lojas filtram `is_contato=false` (follow-up da higienização) — mergeado + deploy confirmado. **Higienização de lojas APLICADA em prod** via MCP: 1.028 contatos criados em `contatos`, 1.028 lojas flageadas, 150 lojas reais, total 1.178 intacto (zero `DELETE`). **Bridge da VPS atualizado e reiniciado** (main `f413e68`, `pm2 restart`, rotas `/api/ifood-api/*` do #743 registradas e gated). **Smokes visuais em prod aprovados:** CRM ("1000 clientes na Consult Delivery", `tenantNome` real) e Lojas (16 ativas, contatos fora). **#746 SQUASH BASELINE CONCLUÍDO (B1 ✅ 100%):** backup id `1042515713` → dump read-only do prod via VPS/Docker (492KB, 177 tabelas/342 policies/6 views/46 fn, bate com prod) → validação em banco zerado (341/342 policies; única falta = `auth.jwt`, inexistente fora da plataforma) → 238 migrations arquivadas em `supabase/migrations_archive/` → repair: `schema_migrations` = 1 linha (`00000000000000`) → verificação: schema e dados 100% intactos. Regra pós-squash em vigor: migrations novas continuam versionadas normalmente. **Única pendência restante do plano:** credenciais `IFOOD_CLIENT_ID`/`IFOOD_CLIENT_SECRET` (Wandson vai passar) → teste live sandbox; Semana 4+ = convergência F3 da coleta (aguarda validação do dicionário no Cowork). Detalhe completo: Tracker (Onde parou + Log de sessões, 2026-07-05 tarde).
- 2026-07-05 (v2.23) — **LEVA 2 do Plano de Continuidade — 5 PRs mergeados hoje de dia (#740-#743 + migration).** **#740** console.css oficializado como DS no CLAUDE.md (decisão travada 04/07 #4 — Semana 3 B2 adiantada e CUMPRIDA). **#741** MIA: sugestões acionáveis (`sugestoes_ia`) como 3ª origem na fila única de aprovações — GAP-3 100% fechado (sem migration nova; RLS já existia). **#742** `data.js` removido (649 linhas mock) + 5 telas órfãs do console clássico (~2.250 linhas de dead code) — B2 cumprido. **#743** iFood API fase 2: rotas bridge gated + flag `lojas.fonte_dados` + módulo de dupla-checagem API×browser com testes — Frente A pronta-para-credencial. Migration `lojas.fonte_dados` **APLICADA em prod via MCP** (1.178 lojas = 'portal', verificado). **Pendências novas para o Wandson:** `pm2 restart bridge-server` na VPS (rotas #743 + endpoints MIA #741) · smoke visual da tela CRM em prod (prop `tenantNome` mudou no #742) · (das anteriores) aprovar SQL do squash · aprovar higienização · credenciais `IFOOD_CLIENT_ID`/`SECRET` no Infisical. Detalhe completo: Tracker (Onde parou + Log de sessões, 2026-07-05 dia).
- 2026-07-05 (v2.22) — **Consolidação da madrugada 04→05/07 — 14 PRs mergeados, Semana 1 do Plano de Continuidade + antecipações (#723/#726-#738).** Executada a Semana 1 do `docs/estrategia/PLANO-CONTINUIDADE-PLATAFORMA-2026-07.md` (decisões travadas 04/07): **#726** auditoria dos GAPs 1-8 do T3 com output bruto de prod (placar inicial 1/4/6/7 fechados, 2/3/5/8 parciais) → todos os parciais fechados na mesma madrugada (**#738** GAP-2 provider+limite de custo, migration `20260705_002` aplicada em prod · **#734** GAP-3 filtros na fila única + 4 telas órfãs removidas · **#733** GAP-4 agregação completa de custos · **#732** GAP-5/8 editar+apagar em Habilidades/Templates · **#736** GAP-6 filtros usuário/período no Audit Log) — **GAPs 1-8 do T3 100% fechados.** **#727** RUNBOOK do squash baseline (Semana 1 B1) mergeado, SQL aguarda aprovação do Wandson (janela domingo 05/07). **#729** higienização lojas/contatos (Semana 2 B3) — análise + migration versionada, não aplicada. **#728** pendências ⚠️ do Wandson consolidadas. **#730** dossiê iFood/SaaS (LGPD/segurança/termos). **#731** iFood API oficial — client OAuth2 + `responderReview` (Frente A antecipada). **#735** roteiro de QA de produção por tela do cv2 (Semana 2 C2). **#737** instrumentação `cost_usd` real em todo `agent_run`. **#723** editar resposta aprovada no painel de avaliações. **Aguarda o Wandson:** aprovar SQL do squash baseline · aprovar aplicação da migration de higienização · confirmar deploy Trigger.dev do #737. Detalhe completo: Tracker (Onde parou + Log de sessões, 2026-07-05 madrugada).
- 2026-07-02 (v2.21) — **Agente GESTOR — F0+F1b+F1 parcial+F2 parcial EM PRODUÇÃO, 11 PRs #681-#691 (T10).** Além da F0 (v2.20): chat `gestor-conversa` na cascata multi-provider Ollama Cloud (kimi-k2.6) → OpenRouter → Anthropic com tools+web search (#683/#685), DELI portada para a mesma cascata (#687), perfil Hermes fase B (#686), Dashboard v1 por loja no Console (#688), `telegram.ts` compartilhado + relatório semanal + esqueleto de coleta diária atrás da trava `GESTOR_COLETA_ATIVA` (#689), rota de aprovação/execução de drafts whatsapp+portal_ifood (#690), fix de segurança removendo secrets hardcoded do painel de avaliações (#691). **Pendente:** gates do Wandson (login único portal iFood + probe do store-switcher + deploy VPS Bridge + rotação da chave Evolution) antes de ligar `GESTOR_COLETA_ATIVA=true` e escalar F2 às 16 lojas.
- 2026-07-02 (v2.20) — **Agente GESTOR (Consultor de iFood IA) — plano aprovado + F0 mergeada (T10 novo).** Plano completo em `docs/gestor/plano-gestor.md`. F0 (PR #681): migrations `20260702_001/002/003` aplicadas (agente `gestor` no catálogo + `agent_chat_messages` + `ifood_portal_nome`/seed 16 lojas) + rota Bridge `/api/portal-worker/run` + `garantirLoja` no `ifood-portal-worker`. Falta o gate do Wandson (probe do store-switcher + login único consolidado) antes de F1.
- 2026-06-23 (v2.19) — **Onboarding de cliente restrito a Avaliação (CSAT/NPS) + aposentadoria do console clássico (sessão 86, branch `claude/gracious-kapitsa-b2daad`, commit `f63c025`):** 4 decisões travadas — **D1** console clássico aposentado para todos (Console v2 = único; `src/App.jsx` sempre renderiza `<ConsoleV2/>`, branch/`<Sidebar>`/`cd-route` default removidos); **D2** gating de módulos por tenant via nova migration `20260622_010_tenant_modules.sql` (APLICADA, RLS + 4 policies + teste de isolamento PASSOU, 0 linhas em prod → não-regressão dos tenants atuais) — `src/console/ConsoleV2.jsx` filtra o menu com design "allowlist quando há linhas; aberto quando não há"; **D3** runbook `docs/runbooks/onboarding-cliente-avaliacao.md` (5 passos: tenant → módulos `visao,csat,nps` → admin via service-role → usuários via `manage-users` → desbloqueio progressivo sem deploy); **D4** módulos do 1º cliente = Visão Geral + CSAT + NPS. Filtro de menu é defesa de UX, não de dados (RLS/RBAC intactos). `npm run build` ✓ (5.30s, sem imports órfãos). Falta o Wandson rodar o runbook p/ o 1º cliente real.
- 2026-06-17 (v2.18) — **Console v2 — Ondas 3+4 COMPLETAS (sessões 62+63):** todas as telas do Console v2 agora com identidade v2 nativa — `LEGADO = new Set([])`. Onda 3 (PR #397, sessão 62): 9 telas migradas de `.cv2-legado` para `.cv2-ct` (`Deli`, `MiaAudit`, `Oracle`, `AgentInbox`, `Heartbeats`, `Metas`, `Memorias`, `Conhecimento`, `Configuracoes`) + 6 bugs corrigidos no code review (filtro `is_consultoria_ativa`, double `cv2-ct` nesting, `tenantDbId` não propagado no Oracle, `loadTickets` sem scope tenant, funções delete sem `r.ok`, `handleInvite` null guard). Onda 4 (PR #401, sessão 63): 3 últimas telas (`CRM` wrapper `margin:-24px -28px`, `Lojas` thin wrapper → LojasListView/LojaWorkspace, `Inadimplentes` rewrite cv2 completo). Merge conflict resolvido (origin/main divergiu). `npm run build` ✓.
- 2026-06-17 (v2.17) — **ESPAÇOS reformulado estilo ClickUp + identidade Console V2 + integração de chat 100% — teste de browser APROVADO** (T1/Chat, sessão 62, PRs #389/#394). Reescrita do módulo ESPAÇOS conforme pedido do Wandson (análise completa + "demandas não vai pra pasta do cliente" + botão de acesso rápido + criar pasta/lista/coluna + identidade V2). **(1)** Migration `20260620_003_espacos_hierarchy.sql` (aditiva/reversível, APLICADA) — hierarquia ClickUp `espacos_folders`/`espacos_lists`/`espacos_columns` (todas `tenant_id NOT NULL REFERENCES tenants(id)` + RLS membership + REPLICA IDENTITY FULL), `client_tasks` estendida com `list_id`/`column_id`, `phase_id` relaxado (reversível); CTE migrou a pasta/lista/colunas da Planet Pizza religando as 15 tarefas (count list_id/column_id NULL = 0/0; isolamento RLS provado). **(2)** `src/lib/api.js` — CRUD da hierarquia + fix do alias de FK `client_tasks_assignee_id_fkey`→`client_tasks_assignee_fkey` (board vazio). **(3)** `src/screens/TarefasClientesScreen.jsx` reescrita dark→V2 (`.cv2-*`), colunas dinâmicas, CRUD inline, drag por `column_id`. **(4)** `src/screens/ChatScreen.jsx` — `espacosHasFolder` gateia botões "Demandas" (deep-link na pasta) e "Espaços" (tela cheia); fix do silent-fail de grupo `whatsapp_groups.group_jid`→`evolution_jid` (#394) + fix TDZ (#393). **Teste de browser (gating) APROVADO 3/3:** Espaços/Demandas abrem com 15 cards na Planet Pizza (grupo); cliente sem pasta (CASA DA BORRACHA) mostra só Departamentos/Bots/Protocolos. PRs #389/#394 squash-merged; deploy live verificado.
- 2026-06-15 (v2.16) — **🏁 Épico "Dashboard iFood" · QA EMPÍRICO E2E DA FASE 6 ENCERRADO → ÉPICO 100% ENTREGUE, EM PROD E VERIFICADO (Fases 0–6)** (T6, sessão 58, doc-only). Fechada a ÚNICA pendência da sessão 56 pela rota (a) escolhida pelo Wandson: **ele dispara o cron no painel Trigger.dev; eu verifico read-only (SQL/API); ele clica Aprovar** — eu não disparo a task. Pegadinha: a 1ª tentativa deu 0 rascunhos porque a task disparada foi a ERRADA (`analise-gerar-relatorio`); re-disparando a CERTA (`radar-diagnostico-semanal`, run `run_cmqemdi4g005b0nn2vx8agals`, COMPLETED/isSuccess, lida pela API v3 singular — v1/v2 dão 404 HTML) os rascunhos apareceram. **METADE 1 (geração, output bruto SQL):** 6 rascunhos p/ a loja `8434cea4-b9c8-41ea-b366-57e8398aad0b`, todos `criado_por_ia=true`/`status='rascunho'`/`metadata.fonte='radar-diagnostico-semanal'`/`aprovada_em=null` (sinais carga_ifood·conversao_baixa·subsidios·cancelamentos·op_atrasos·op_chamados; op_online & op_canc_super não dispararam — correto). **METADE 2 (aprovação, output bruto SQL):** Wandson clicou Aprovar em "Tratar 7 cancelamento(s)" → `status='aprovada'`+`aprovada_em='2026-06-15 03:14:04.1+00'`+`updated_at='…03:14:04.669873+00'`, os outros 5 intactos em `rascunho` (prova que `aprovarTarefa`/#361, escopada por `loja_id`+`status='rascunho'`+`criado_por_ia`, move só o card clicado). **3/3 critérios de aceite provados por output bruto** (rascunho na loja certa · aprovar move status · nada vira tarefa "valendo" sem o clique do Wandson). **Balde 3** segue DEFERIDO (precisa `.xlsx` real — anti-padrão P1), ou novo pedido. Branch `wandson/dashboard-ifood-fase6-qa` (fresca de `origin/main`, caso #155); nenhum código tocado.
- 2026-06-15 (v2.15) — **Integração VendaERP · FASE 2 (escrita com confirmação no Telegram, padrão propor→confirmar) IMPLEMENTADA E COMMITADA** (T4/T6, sessão 54, branch `wandson/vendaerp-fase2-escrita` — falta SÓ o GATE 0 do Wandson). Padrão de 2 etapas com o "sim" mediado pelo agente: **5 tools `erp_propor_{oportunidade,lancamento,boleto,nfe,estoque}`** no `vendaerp-mcp/` (validam args via Zod `inputShape`, gravam proposta `status='pending'` em `vendaerp_proposals` com `expires_at=now()+10min`, retornam `{proposal_id, resumo, expires_at}` — **nunca executam**) + **`erp_confirmar(proposal_id)`** (transição **atômica `pending→confirmed`** por PATCH condicional uso-único → despacha ao Bridge `POST /api/vendaerp/<op>` com `x-internal-token` → escreve no ERP → `executed`/`failed`; expirada → `expired`). **Bridge:** 5 rotas POST + funções de escrita **SEM `withRetry`** (`bridge-server/{routes,lib}/vendaerp.js` — POST não-idempotente). **Migration `20260614_003_vendaerp_proposals.sql`** (tenant-scoped + RLS por membro + CHECK status + índice parcial pending). **Testes:** offline `vendaerp-mcp/test/smoke.js` **6/6** + Bridge `bridge-server/test/vendaerp-write.test.js` **14/14** (live `write-live-smoke.js` reservado ao GATE 0). 8 commits (`e2a913e`→`2ba8066`). **➡️ GATE 0 do Wandson (secrets/VPS):** `systemctl restart hermes-gateway` + sessão nova do bot · `npm run write-live-smoke` (output bruto: proposta `executed` + `audit_log mcp:erp_confirmar ok=true`) · E2E Telegram (propõe→pergunta→"sim"→grava) · **rotação do `VENDAERP_TOKEN`**. **Dívidas técnicas (Tracker §15b):** shape PascalCase do body do POST pendente do 1º live; payloads tolerantes/passthrough até o ERP fixar o contrato; reconciliação de propostas órfãs (`pending` expiradas — só `expires_at`, sem sweeper). Spec/plano em `docs/superpowers/`. Próxima = Fase 3 (multi-tenant). _(Nota: esta é a Fase 2 da **VendaERP**, distinta da Fase 2 do épico Dashboard iFood.)_
- 2026-06-14 (v2.14) — **Épico "Dashboard iFood" · polimento pós-épico + fechamento (sessão 56)** (T6). (1) **Endurecimento defense-in-depth (#361 `9559cfa`):** `aprovarTarefa`/`rejeitarTarefa` (`src/lib/api.js`) + 2 call-sites no `RadarReal.jsx` passam a escopar a transição por `loja_id` (`.eq('loja_id', lojaId)`) além de `id`+`status='rascunho'`+`criado_por_ia` — fecha a brecha teórica de aprovar/rejeitar rascunho de OUTRA loja. Deploy frontend (GitHub Pages, bundle `index-CID8E-Vg.js`), QA por string OK (sem regressão). (2) **QA empírico de fim a fim da Fase 6 = a ÚNICA pendência do épico:** 6/8 sinais provados read-only; a metade de **escrita-em-prod** (rascunho aterrissando + transição persistida) foi bloqueada pelo classifier de auto-mode (insert de QA autoria-agente em prod não autorizado) → **decisão do Wandson** sobre a rota: (a) disparar o cron `radar-diagnostico-semanal` já deployado (rascunhos reais + 1 notificação interna, ~US$0,002, precisa `TRIGGER_SECRET_KEY`; outward-facing → exige `ok`) · (b) esperar o cron de 2ª 08:00 · (c) autorizar INSERT de QA + provas de transição. (3) Fechamento doc-only (Tracker + PLANO-MESTRE + `WikiBrain/log`) na branch `wandson/dashboard-ifood-fechamento-epico`. **NÃO declarado "feito" o QA empírico** (anti-padrão 10 — a escrita não foi persistida).
- 2026-06-14 (v2.13) — **🏁 Épico "Dashboard iFood" · FASE 6 ENTREGUE — ÉPICO COMPLETO (Fases 0–6)** (T6, sessão 55; parser Trigger.dev + frontend + migration de idempotência). Demandas como **rascunho automático + aprovação** em `tarefas_loja`: `diagnostico-semanal.ts` deriva até **8 sinais** das métricas EAV e cria rascunhos (`status='rascunho'`, `criado_por_ia=true`, NOT NULL preenchidos, `metadata.origem`), com dedup SELECT-then-filter por origem + **backstop** índice único parcial `uq_tarefa_ia_origem_ativa` (migration `20260620_002` APLICADA; `23505` = no-op); painel **"Ações recomendadas"** no `RadarReal.jsx` (Aprovar/Rejeitar via `listTarefasIA`/`aprovarTarefa`/`rejeitarTarefa` em `api.js`, update guardado por `.eq('status','rascunho')` → RLS). Review LOCAL cd-lens xhigh 4/5 tratados (dívida aceita do `cargaPct`); `tsc`/`vite` EXIT 0. Aceite cumprido: nada vira tarefa "valendo" sem o clique do Wandson. **Balde 3 DEFERIDO** (quebra fina por colunas — gated por P1, precisa `.xlsx` real). Próxima ação deste épico = nenhuma (aguardar planilha real p/ Balde 3 ou novo pedido).
- 2026-06-14 (v2.12) — **Épico "Dashboard iFood" · FASE 4 ENTREGUE** (T6, sessão 51, só frontend — sem migration/sem deploy). `src/console/RadarReal.jsx` ganhou o **filtro temporal universal**: card de filtros de 2 linhas (loja + período) com botões-janela `Tudo · Dia · Semana · Mês · Personalizado` (reusa `.cv2-btn`/`.cv2-btn.sec`) + date-range no modo Personalizado. `calcPeriodo` traduz a janela em `{inicio,fim}` e o `carregar` passa `periodo` ao `lerMetricas` (ramo dormente desde a Fase 1) → filtra `data_ref` no servidor + dedup por `data_ref desc` (snapshot certo do período, não o último upload). **Honestidade do grão (anti-padrão P1):** aviso fixo de que o iFood entrega agregado por período (filtro recorta pela `data_ref`) + "mostrando dados até DD/MM/AAAA"; janela vazia → banner honesto "Nenhum relatório do iFood nesta janela" (nunca tela em branco/dado fabricado). esbuild EXIT 0 (852.6kb). Review LOCAL (cd-lens): 0 críticos/0 altos/1 médio (copy, corrigido)/2 baixos. Próxima = Fase 5 (série diária de verdade `radar_series` — Migration B + deploy Trigger.dev, começando por Operação).
- 2026-06-14 (v2.11) — **Épico "Dashboard iFood" · FASE 3 ENTREGUE** (T6, sessão 50, só frontend — sem migration/sem deploy). `src/console/RadarReal.jsx` ganhou a grid **"Saúde financeira & cruzamentos"** com **7 KPIs derivados** das chaves REAIS do EAV (receita líquida via `conciliacao_repasse_liquido` autoritativo c/ fallback entrada+taxas+subsídios · carga total iFood % · taxa de cancelamento % · R$ perdido em cancelam. · visitas não convertidas · avaliação vs piso Super 4,5 · negociações), cada um guardado por `!= null` (**some quando falta a fonte, nunca zera**) + sinais enriquecidos (cancelamentos: motivo top/contestáveis/R$ que a Defesa contesta; chamados). Anti-padrão P1 respeitado (zero chute de coluna; `PISO_SUPER=4,5` é regra de negócio rotulada, não dado). esbuild EXIT 0 (15.9kb). Próxima = Fase 4 (filtro temporal universal: UI de janela de período + ativar o ramo `periodo` do helper `lerMetricas`, dormente desde a Fase 1).
- 2026-06-14 (v2.10) — **Épico "Dashboard iFood" · FASE 2 FECHADA E VALIDADA EM PROD** (T6, sessão 49, #349, deploy `20260614.33`). O fix `data_ref` da Operação da sessão 48 (`9216fd2`) estava QUEBRADO — o `parsePeriodo` ESTRITO não casava o label `"Diário (09/05 a 07/06)"` (formato curto sem ano) → 14 métricas `operacao_*` continuavam com `data_ref` = hoje. Corrigido (`1f728a5`) com 2º ramo p/ formato curto + inferência de ano, sem fabricar dado. **Gate validado por output bruto:** `operacao.data_ref=2026-06-07`, métricas novas presentes (operação 14/cardápio 6/vendas 4), nenhuma antiga sumiu, `repasse_liquido=8427.01`/`logistica_pedidos=445` íntegros. Próxima = Fase 3 (cruzamentos no `RadarReal.jsx`, só frontend).
- 2026-06-14 (v2.9) — **Épico "Dashboard iFood" · FASE 2 entregue** (T6, sessão 48). Parser `processar-fontes.ts` enriquecido com **7 famílias de métricas aditivas via EAV** (repasse líquido + lançamentos por tipo da conciliação · cancelamentos contestáveis · cauda-longa do cardápio · cancelamentos Super absoluto + Meta real de Operação · turnos/serviço da logística) + 2 fixes (`data_ref` de Operação no período real; `operacao_metas` com `valor_texto`). Diff +44/−3 só no parser (`b398509`+`9216fd2`). **Merge-first** p/ driblar a corrida deploy-vs-reset que empacotou o deploy `20260614.21` sem a Fase 2 (provado por SQL bruto). Deploy + validação empírica (o gate) no fechamento da sessão. Próxima = Fase 3 (cruzamentos no `RadarReal.jsx`, só frontend).
- 2026-06-14 (v2.8) — **Épico "Dashboard iFood" · FASE 1 entregue** (T6, sessão 46). Dedup de `radar_metricas` unificado em 2 helpers `lerMetricas` (`trigger/_shared/radar-metricas.ts` + `src/lib/radar-metricas.js`); os 4 call-sites trocaram o bloco inline pela chamada sem `periodo` → **diff de comportamento NULO** (verificado por review LOCAL). + guarda `selectEfetivo` (anti-vazamento cross-tenant, no-op hoje, blinda Fase 4). Build vite exit 0 · tsc exit 0 · deploy Trigger.dev. Próxima = Fase 2 (enriquecer parser).
- 2026-06-14 (v2.7) — **Épico "Dashboard iFood — extração máxima" iniciado · FASE 0 (fundação temporal) entregue** (T6, sessão 45). Migration A `20260615_001_radar_temporal.sql` (colunas `granularidade`/`data_ref` + índice de dedup temporal, aditiva/reversível, APLICADA) + parser preenche o grão no insert (deploy Trigger.dev `20260614.12`); verificado E2E (2 meses → 2 `data_ref` distintos). Plano de 7 fases em `/root/.claude/plans/dynamic-swimming-zebra.md`; próxima = Fase 1 (helper único de dedup, comportamento-neutro).
- 2026-06-08 (v2.6) — **D6 gravada nas Decisões Travadas** (aprovada 2026-06-07 + **reaberta pelo Wandson 2026-06-07** pós-F1; detalhe no Tracker). Fecha pendência da sessão 11.
- 2026-06-06 (v2.5) — **FASE 2 onda 1 APLICADA** (SQL aprovado #161 → aplicada via D5 v2; validações + teste de isolamento). **D5 v2:** Wandson liberou merge + aplicação de migrations aprovadas pelo Cowork. Checklist mestre detalhado movido para os docs da FASE 0/1 (histórico git ≤ v2.4).
- 2026-06-06 (v2.4) — equipe = 1 pessoa; DELI = COO travado; CHECKPOINT 1 go.
- 2026-06-06 (v2.3) — #156 e #152 merged; #155/#157 → #158; regra "branch nova por entrega".
- 2026-06-06 (v2.2) — D5 v1 registrada.
- 2026-06-06 (v2.1) — FASE 0 publicada (#156); FASE 1 reconciliada (D4/B, `0f62ebb`).
- 2026-06-06 (v2) — fusão PLANO-MESTRE + mapa-vivo na raiz.
- 2026-06-03 — D1/D2/D3 decididos. · 2026-06-02 — plano persistido.

node_modules/
dist/
.env
.env.local
chats/
project/
.mcp.json
.claude/settings.local.json

# === Supabase ===
supabase/.temp/

# === Wiki-brain ===
# Dados crus (transcrições, PDFs, áudios — podem ter info de clientes)
WikiBrain/raw/

# === Obsidian plugins ===
# Binários de plugin são grandes e recriáveis via install
WikiBrain/.obsidian/plugins/

# === Graphify ===
# Saída gerada do graphify (recriável, não versionar)
graphify-out/

# === Credenciais e env ===
*.key
*.pem
secrets/

# === Backups de banco (contêm API keys — não subir no GitHub) ===
backup_*.sql

# === OpenClaw config local ===
# Configs com tokens
.openclaw/openclaw.json

# Pastas pessoais e extrações (não pertencem ao repo)
# WikiBrain ignorado, EXCETO WikiBrain/wiki/ (Tracker + docs de track versionados)
WikiBrain/*
!WikiBrain/wiki/
Plataforma Consult Delivery-handoff*/
_handoff_extracted/

# Backups de arquivos editados (geral)
*.bak

# Obsidian — estado de workspace (pessoal)
WikiBrain/.obsidian/workspace.json
WikiBrain/.obsidian/workspaces.json
WikiBrain/.obsidian/workspace-mobile.json

# Vídeos locais
my-video/

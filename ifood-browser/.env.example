# Copie para .env e preencha. NÃO commitar o .env real (já no .gitignore do repo? confira).
# Auth da web UI do navegador ao vivo (viewer + take-over).
IFOOD_BROWSER_USER=consultor
IFOOD_BROWSER_PASSWORD=troque-isto-por-uma-senha-forte
